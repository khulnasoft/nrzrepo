{
  "name": "with-docker",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "nrz run build",
    "clean": "nrz run clean",
    "dev": "nrz run dev",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "nrz run lint",
    "test": "nrz run test"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^3.2.5",
    "nrz": "^2.0.3"
  },
  "packageManager": "yarn@1.22.19",
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=7.0.0"
  }
}
